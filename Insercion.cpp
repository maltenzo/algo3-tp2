insercion(g, l){
  res=[1, 2, 3];
  libres =[4........n];
  while(res.size() <= g.size()){
    v = elegir(g, libres);
    res.push_back(v);
  }
  return res;
}

elegir(g, libres){
  res = 0;
  if (MB){

  }
  else if (MC){

  }
  else if (ML){

  }
  return res;
}
